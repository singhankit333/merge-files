#include "FileMerger.h"
#include <iostream>

void FileMerger::mergeFiles(const std::string& inputDir, const std::string& outputFile) {
    // Placeholder for merging logic
    std::cout << "Merging files from directory: " << inputDir << " into " << outputFile << std::endl;
}